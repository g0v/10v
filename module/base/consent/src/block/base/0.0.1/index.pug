div
  plug(name="widget")
  script(type="@plotdb/block"): :lsc
    module.exports =
      pkg: {name: "consent", path: "block/base/0.0.1/index.html"}
      init: ({root}) ->
        @ldcv = new ldcover {
          root: root.querySelector('.ldcv')
          escape: false
        }
      interface: ->
        get: ~> @ldcv.get!

